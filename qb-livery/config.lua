Config = {}

-- Require the player to be in the driver seat to open /livery
Config.RequireDriver = true

-- Optional: restrict to jobs. Example: { police = 0, mechanic = 0 }
-- Empty table = everyone can use.
Config.AllowedJobs = {}

-- Use qb-menu for the UI
Config.UseQbMenu = true

-- If true, also include a 'Remove Livery (Stock)' option for mod-type liveries (type 48).
Config.ShowStockForModType = true
